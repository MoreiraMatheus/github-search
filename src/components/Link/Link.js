import styled from "styled-components";

const Link = styled.a`
    font-weight: bold;
    font-size: 1.2em;
    text-decoration: none;
    &:hover{
        color: green;
        transition: 0.1s;
    }
`
export default Link