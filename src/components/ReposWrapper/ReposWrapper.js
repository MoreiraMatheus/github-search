import styled from "styled-components";

// transformar esse componente de forma que ele seja reutilizável
const ReposWrapper = styled.div`
    width: 50%;
    height: 100%;
`
export default ReposWrapper;